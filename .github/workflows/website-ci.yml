name: website
on:
  push:
    paths:
      - "website/**"
      - "topics/**"
      - "drishti/**"
  pull_request:
    paths:
      - "website/**"
      - "topics/**"
      - "drishti/**"
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: website
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: website/package-lock.json
      - run: npm ci
      - run: npm run catalog
      - run: npm run catalog:drishti
      - run: npm run sync:drishti
      - run: npm run test:links
      - run: npm run test:drishti-pass
      - run: npm run build
