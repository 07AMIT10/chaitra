---
title: Bayesian Distributed Systems
---

import ReadmeBody from "./readme-body.md";
import BayesianDistributedLab from "../../../components/BayesianDistributedLab.tsx";

## Cluster belief network

<Callout variant="note" title="How to read this topic">
  Follow the narrative first, then open the **Lab** section for the **switch → servers → symptoms**
  belief graph. Tune **P(switch faulty)** and slow-server likelihoods, click **telemetry chips**
  (500ms pings, DB timeout, user fail), try **Both servers slow**, **DB + user fail**, or
  **Single server alarm** presets, predict whether you'd **reboot Server A** when both alarms fire,
  then reveal **deterministic reboot vs Bayesian route-around-switch**.
</Callout>

<ReadmeBody />

<h2 id="lab">Lab</h2>

<BayesianDistributedLab client:visible />
