---
title: Bloom Filters
---

import ReadmeBody from "./readme-body.md";
import BloomFilterLab from "../../../components/BloomFilterLab.tsx";

## The probabilistic bouncer

<ReadmeBody />

<h2 id="lab">Lab</h2>

<p>
  Tune <strong>m</strong> (bits), <strong>n</strong> (inserted keys), and <strong>k</strong> (hash
  functions). The live bit array uses the same sizing formulas and MD5 hashing as{" "}
  <code>bloom_filter.py</code> in the repo.
</p>

<BloomFilterLab client:visible />
