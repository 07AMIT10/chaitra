---
title: Raft vs Gossip
---

import ReadmeBody from "./readme-body.md";
import RaftVsGossipLab from "../../../components/RaftVsGossipLab.tsx";

## Raft vs gossip

<Callout variant="note" title="How to read this topic">
  Follow the narrative first, then open the **Lab** section on the same cluster topology. Toggle
  **Raft** (leader + quorum) vs **Gossip** (epidemic fan-out), step replication or infection rounds,
  and compare message load. Use **Etcd**, **Cassandra scale**, or **Raft O(N) load** presets, predict
  quorum commit or full spread within the suggested steps, then reveal ground-truth metrics.
</Callout>

<ReadmeBody />

<h2 id="lab">Lab</h2>

<RaftVsGossipLab client:visible />
