---
title: Randomized Algorithms
---

import ReadmeBody from "./readme-body.md";
import RandomizedAlgorithmsLab from "../../../components/RandomizedAlgorithmsLab.tsx";

## Las Vegas vs Monte Carlo

<Callout variant="note" title="How to read this topic">
  Follow the narrative first, then open the **Lab** section for the same toy problem — find the **k-th
  smallest** in a 12-element array. Switch **sorted vs shuffled** input, tune **k**, **RNG seed**, and
  **Monte Carlo probe count**, watch **Las Vegas** comparisons vs **fixed MC reads** and the **first-pivot**
  baseline, try **Adversarial sorted**, **Shuffled**, or **MC many probes** presets, predict whether
  deterministic pivot work exceeds randomized quickselect, then reveal **Las Vegas vs Monte Carlo vs exact**.
</Callout>

<ReadmeBody />

<h2 id="lab">Lab</h2>

<RandomizedAlgorithmsLab client:visible />
