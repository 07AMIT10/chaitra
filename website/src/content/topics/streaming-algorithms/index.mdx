---
title: Streaming Algorithms
---

import ReadmeBody from "./readme-body.md";
import StreamingAlgorithmsLab from "../../../components/StreamingAlgorithmsLab.tsx";

## One stream, three sketches

<Callout variant="note" title="How to read this topic">
  Follow the narrative first, then open the **Lab** section to scrub a synthetic **router packet stream** and
  query an IP against **Count-Min**, **HyperLogLog**, and **Bloom** in parallel. Compare each sketch to
  **ground truth** from the same prefix, try **DDoS heavy hitter**, **Early stream**, or **Long-tail probe**
  presets, and predict whether CMS will **overestimate** and Bloom can false-positive before revealing metrics.
</Callout>

<ReadmeBody />

<h2 id="lab">Lab</h2>

<StreamingAlgorithmsLab client:visible />
